# KR-004 — Implement local schema and prove RLS and gateway trust boundaries

## Goal

Prove household isolation before any production table exposure.

## Context

docs/product-specs/BACKEND.md is a normalized logical design and RLS matrix, not deployed SQL.

**OD-42, owner approval 2026-09-10:** local migrations and executable database isolation tests for **AC-1–4 only** are now authorized before KR-003 completion. This is a prospective exception to the operational hold, not a KR-003 closure, private-alpha distribution decision, deployment or production-enforcement approval. AC-5–7, KR-005/006 and real family use remain outside this authorization. Visual work is paused and preserved.

## Current execution — 2026-09-11

**AC-1–4 implemented and locally tested; issue remains In Progress, not complete.**
The native Windows Docker client now independently reaches the explicit local Linux-engine named pipe. A newly allocated, labelled, network-isolated task container ran the versioned migration and **288 actual pgTAP assertions (243 RLS + 45 constraints), all PASS with zero skips**. Ten Node runner-safety tests also passed. Exact runtime/target identity, hashes, failed development attempts and verified removal of task-owned synthetic data are retained in [execution evidence](../../test-plans/evidence/KR-004-LOCAL-DB-2026-09-11.md).

Existing draft PR #17 / `kr-004-local-schema-rls` remains stacked on the KR-003 branch. No PR #16 merge/rewrite, remote DB, real identity, host repair, device operation or visual work occurred. SQL claims exercise real database roles and the image's Auth SQL; they do not prove HTTP/JWT verification or gateway authorization. There is no application control RPC yet. AC-5–7 remain open and outside the current authorization; continuing into them requires owner direction.

## Historical local prerequisite evidence — 2026-09-10

Verified clean baseline `2f5be2fb8d62eae3aeaf8a8662b0e976844db904`; separate branch `kr-004-local-schema-rls` is stacked on `kr-003-mi8-overlay-stability` at that commit, not included in PR #16's changes. No existing KR-004 branch/PR existed.

Read-only checks actually executed:

| Check | Observed result |
| --- | --- |
| `command -v psql`, `postgres`, `initdb`, `supabase` and conventional installed PostgreSQL directories | No usable PostgreSQL binaries or Supabase CLI found in WSL |
| `docker version` | Exit 1; Docker shim reports WSL integration unavailable |
| Windows `Get-Command` for `docker.exe`, `psql.exe`, `postgres.exe`, `initdb.exe`, `supabase.exe` | Docker client available; remaining commands missing |
| `docker.exe version --format '{{.Client.Version}} {{.Server.Version}}'` | Exit 1; client 29.6.2; no server version; `dockerDesktopLinuxEngine` local named pipe not found |

**Historical blocker, resolved 2026-09-11:** no available local database execution runtime on 2026-09-10. An installed Docker client was not a running DB/engine. No remote endpoint was queried, no host privilege/integration/service change was made, and no unrelated container/volume or real account was touched. At that point no disposable target existed and migrations/database tests were unimplemented/unrun. Mocks, CI scaffold checks and documentation were not database isolation PASS evidence.

Unblock by making the existing local Docker Desktop Linux engine available, or providing an authorized installed local PostgreSQL toolchain. Recheck endpoint/local ownership and create a uniquely named task-owned disposable target before implementation/execution; never reset an existing arbitrary database. Exact PostgreSQL/Supabase tooling choice remains unselected. Continue the approved AC-1–4 slice only after that prerequisite is available.

## Scope

Create local-only versioned migrations and SQL allow/deny fixtures for parent households/devices/policy/grants/receipts and private credentials/pairing/push/outbox/audit. Define typed transactional function boundaries and test privileged gateway predicates.

## Out of scope

Production table exposure/deployment, remote production reset, extra databases, final mobile features.

## Dependencies

KR-001, KR-002 See [backlog](../ISSUES.md).

## Technical constraints

Create local-only versioned migrations and SQL allow/deny fixtures for parent households/devices/policy/grants/receipts and private credentials/pairing/push/outbox/audit. Define typed transactional function boundaries and test privileged gateway predicates.
Follow linked ADRs and the repository task contract. No unapproved product assumptions or production gate bypass.

## Security/privacy

Service keys bypass RLS; verify scoped elevated transactions independently and lock down definer helpers.

## Acceptance criteria

- [x] AC-1: Migrations build the model from empty local DB with keys, composite tenant FKs, uniqueness, numeric constraints and needed indexes.
- [x] AC-2: All parent-exposed tables enable RLS with minimum grants in the same migration; private tables/functions have no anon/authenticated broad access.
- [x] AC-3: A/HA/DA and B/HB/DB fixtures prove own access and deny foreign access for SELECT/write/join/RPC and membership self-promotion.
- [x] AC-4: Tests use real client roles/auth claims rather than only service-role; no-policy/null-auth/removed-membership cases fail closed.
- [ ] AC-5: Device gateway contract derives target from credentials and has negative HTTP tests/stubs for foreign targets and parent privilege attempts.
- [ ] AC-6: Control transaction commits version, grant, command and outbox atomically; rollback leaves no partial grant.
- [ ] AC-7: Migration/reset instructions are explicitly local and all required checks run before any client exposure.

## Unit test plan

Payload bounds and domain-independent validation fixtures.

## Integration test plan

Supabase SQL/pgTAP tests plus HTTP gateway isolation tests; DB-01–04 and AUTH-01–06.

## Physical-device test plan

Not applicable to schema; child route integration uses synthetic credentials.

## Failure-injection test plan

Transaction rollback, duplicate ID, concurrent writes, forgotten grants/policies and stale membership JWT.

## Observability

SQL/HTTP test evidence and query plans; secrets redacted.

## Definition of done

Local migration and allow/deny suites pass; trust boundaries match docs; no production exposure.
Run relevant tests, record actual evidence, update source-of-truth docs/ADRs and list unrun checks.
Do not report completed merely because a plan exists.

## Done when

Local migration and allow/deny suites pass; trust boundaries match docs; no production exposure.

## Story points

5 (relative estimate, not hours).

## Planning fields

Priority: P0

Risk: High

Milestone: Architecture & Feasibility

Area: Backend

Platform: Backend

Work Type: Implementation

Status: In Progress

Decision required: No

Recommended labels: area:backend, security, privacy

## Open / UNSPECIFIED questions

Hosting region/tier, Supabase CLI and production runtime remain **UNSPECIFIED**. The local test image and executed AC-1–4 results are pinned/recorded above. Auth/HTTP/gateway and control transactions remain unimplemented/unrun; no later acceptance criterion is inferred from database isolation PASS.

## Links to ADR/spec

- [docs/product-specs/BACKEND.md](../../../docs/product-specs/BACKEND.md)
- [docs/adr/0006-backend-and-sync.md](../../../docs/adr/0006-backend-and-sync.md)
- [docs/SECURITY.md](../../../docs/SECURITY.md)

## Authoritative documentation

Verified/reviewed 2026-09-05; recheck before implementation.
- [Official reference 1](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Official reference 2](https://supabase.com/docs/guides/database/testing)
- [Official reference 3](https://supabase.com/docs/guides/local-development/database-migrations)
- [Official reference 4](https://supabase.com/docs/guides/getting-started/api-keys)

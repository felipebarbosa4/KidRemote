# OD-51 exact Samsung IDS runtime classification

- **Goal:** Classify the single structural file observed by probe `88af2a3b-bdfd-4d5b-b814-729facbac4a9` and, only if the evidence supports it, admit it as runtime metadata for the exact authorized Samsung configuration so the product slice can resume from enrollment.
- **Context:** The current read-only observation proved exact LAB/fixture/configuration provenance and found four known AndroidX runtime files plus only `shared_prefs/android.app.ActivityThread.IDS.xml`. Prior immutable product evidence has no backend device, saved-device pointer, identity, pending pairing or accounting state. KR-003 and KR-010 remain the bounded product slice dependencies.
- **Constraints:** Do not read the XML, touch ADB/Samsung/backend state, edit the probe result or either historical INVALID attempt, generalize to another Samsung/Android build, change the generic ProductRuntime catalog, weaken symlink/nonregular/unknown-file rejection, or put OEM evidence logic in the APK.
- **Done when:** Source, resources, exact approved APK, DEX and bundled dependency bytecode have reproducible negative results for the IDS identifiers; bounded non-official corroboration supports an OEM-runtime origin; an exact-configuration host overlay and fail-closed regressions pass on PS5.1/PS7 and required CI; a separate immutable classification review is recorded; only then may a new product-slice bundle be frozen for one owner run.

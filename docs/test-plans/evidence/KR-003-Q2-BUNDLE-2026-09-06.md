# KR-003 Q2 calibration-only handoff — 2026-09-06

- **Goal:** Capture deterministic recovery-phase evidence with clean finalization before any 100-sample run.
- **Context:** [Stopped calibration](KR-003-CALIBRATION-2026-09-06.md): physical Settings PASS, uncorroborated software state, then an empty-row reporting defect.
- **Constraints:** KR-003 only; owner-operated Windows ADB; original run/bundle immutable; no production enforcement or safe-package changes; no KR-004.
- **Done when:** New calibration records physical observations, phase telemetry and restored-radio readback, or stops with preserved evidence. This handoff alone is not physical validation.

## Verified source and bundle

Source/build commit: **`37ad70b177d84d21250887c4b75e33c1b6d328af`** on `kr-003-mi8-overlay-stability`.
New bundle: **`C:\platform-tools\kr003-qualification-bundles\37ad70b`**.
Created `2026-09-06T17:28:01.065Z`, after exact-head CI passed. The source worktree was clean before and after packaging.

| File | SHA-256 |
| --- | --- |
| `bundle.json` (file-hash index; not an archive) | `825215079c7580a2fea23c8f327464586be5f71b3a582424d7836857b59c237b` |
| `Start-KR003.ps1` | `b67bd7925e2e5c92bfe2fb9e9334263fab9998087e7324677d3009c011bf1e81` |
| `Qualification.psm1` | `496ad8a7c26aed191fb7e14f77bdbb41cb9326f30f05b7ac44a84ba6e6dc5909` |
| `protocol.md` (KR003-Q2) | `53fa2e310c6ba94426c7538d0c79cf0a3a638db17b7793e2c32d5ae834162e2e` |
| `candidate.apk` | `1c97a382d686c209676f76893e4a7b404853d6481bd30d3cb33e423943617630` |
| `ordinary-fixture.apk` | `6653f10b527cc9a273a8c0ea045cd250f6978c1acfb8e92b00b701f6c14f84bb` |

All six mounted files were re-read; each indexed hash was verified and runner/module/protocol bytes matched `git show` for the source commit.
The original d81f19a bundle and all twelve original failed-run files were independently hash-verified unchanged. This new candidate APK adds only
bounded debug recovery/owned-window instrumentation; the original resolver, surface policy and timer files still match `de941a9`.
The bundle identity remains this source commit even when a later documentation-only commit publishes this handoff.

## Executed checks

[CI run 34048490604](https://github.com/felipebarbosa4/KidRemote/actions/runs/34048490604) completed **success** with exact head SHA `37ad70b177d84d21250887c4b75e33c1b6d328af`:

- `validate`: repository validation, six Node evidence/negative-security tests, 45 qualification assertions and 65 finalizer/recovery assertions.
- `Android enforcement spike`: testDebugUnitTest, lintDebug, assembleDebug, lintRelease, assembleRelease, merged-permission/release-DEX audit;
  23/23 JVM cases executed, no failures/skips. Both candidate and fixture variants built/linted. Release has no configured separate JVM test task.
- `Windows PowerShell runner tests (no device)`: 65 assertions passed under **Windows PowerShell 5.1.26100.33296**. Device commands were stubs.

The same relevant suites passed locally, including Gradle's registered `test` aggregate, native Linux PowerShell 7.6.5, Node checks, release audit,
`node tools/validate.mjs` and `git diff --check`. Packaging repeated build/lint/JVM/audit/repository checks successfully. No physical device command,
installation or new observer result was generated by these checks; Q2 calibration remains **Not run**.

## One PowerShell command — calibration only

With the authorized Mi 8 connected, unlocked and visible:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\37ad70b\Start-KR003.ps1" -OfflineNetwork -CalibrationOnly
```

No manual copying/hashing/install commands are needed. The runner verifies bundle hashes, installs in place without uninstall/data-clear,
verifies installed APK hashes, exports prior metrics, checks disclosed permissions/health, records original radio flags, temporarily disables
only enabled Wi-Fi/mobile flags, performs the lab sequence and independently attempts restoration/readback. Process-scoped ExecutionPolicy
does not modify host policy. The old run's final Wi-Fi/mobile flags remain **UNSPECIFIED**; this new run cannot retroactively prove their restoration.

1. Confirm once with P that the lab has no alternate Internet connection when prompted.
2. Watch one fresh ten-second expiry and **at least ten continuous blocked seconds**, no flicker/disappearance/ordinary use; press P only for that observation.
3. At the recovery prompt, press Home physically once and verify restriction remains. Tap the overlay's **Open device settings**; verify Settings
   actually opens and is usable. Press P only if both succeeded. **Stay in Settings until the runner navigates**; phase state is collected while you observe.
4. If corroboration succeeds, the runner opens the ordinary fixture; confirm visible re-enforcement with P. After automatic Clear, tap
   **Test ordinary use** once. The fixture itself records receipt; no repeated labels or log transcription is required.
5. The runner verifies end configuration/hashes, restores/readbacks radios and writes reports, then stops. **No qualification sample starts even on success.**

Estimate **3–5 minutes**, not a measured duration. F = observed failure (then a short reason choice), I = invalid/missed observation, Q = stop.
Flicker/no-block/escape/unusable recovery or software enforcement/health failure stops. Physical Settings P without correlated safe evidence stops
**INVALID:SETTINGS_RECOVERY_ORACLE_UNCORROBORATED** while retaining OWNER_PASS. An invalid/failed calibration is not automatically replaced.
Exit 0 with `CALIBRATION_COMPLETED_ONLY` means only calibration succeeded and cleanup/reporting completed; it is not qualification.

## Evidence and remaining boundary

New evidence is written under `C:\platform-tools\kr003-qualification\run-*`; the agent reads it directly from `/mnt/c`. No log copying/pasting is needed.
The JSON/CSV journal retains calibration and any partial current attempt. `safety-calibration.json` records owner response/time, phase/revision/trace
bounds, software corroboration and re-entry/Clear; telemetry includes only typed debug-owned window diagnostics. `network-restoration.json` records
each original/observed flag. `summary.json` and `SUMMARY.md` preserve the primary reason and separate secondary finalization errors, even with zero rows
on writable storage. Hard process/host termination or unwritable storage cannot guarantee recovery/reports.

Original evidence directory: `C:\platform-tools\kr003-qualification\run-20260906-025608-9dd0c8c7`; twelve original files retained, including manifest,
calibration, attempts, telemetry/trace/fixture, prior metrics, network-original/touched and two verified APKs. No original summary/safety/restoration
readback was written; it has not been retroactively manufactured.

The original OWN_PACKAGE fix remains successful on recorded Mi 8 observations, with **12 independent observed post-fix expiries across builds**
(one initial, ten checkpoint, one stopped calibration). Home/Settings are same-session observations, not extra expiries. This does not validate
the unresolved recovery-state discrepancy, Q2 on-device behaviour, 100 samples, API 28/35/36 or another OEM, lifecycle/permission/tamper/safety rows,
or Play acceptance. KR-003 remains Open/In Progress; PR #16 remains draft; KR-004 remains Backlog and untouched.

After this calibration, inspect its physical/phase/readback evidence before any separate 100-sample command. The full [Q2 contract](../KR-003-QUALIFICATION.md)
still requires its own calibration plus 100 fresh independent physical observations, each with at least ten blocked seconds and a paired monotonic
attachment measurement, zero enforcement failures and p95 ≤2,000 ms on the specific configuration. Do not lower that gate or pool earlier observations.

# KR-009 OD-47 local control/sync/ACK evidence

Date: 2026-09-13 America/Toronto; runs continue into 2026-09-14 UTC. Baseline `cde74f626c9e1c4525562e041338f9f051a2973d`, branch `kr-009-local-sync`, draft PR #22 targeting `kr-008-local-accounting`, existing Issue #9. OD-47 was recorded before implementation. Earlier PRs and historical evidence are unchanged.

## Scope and observed implementation

Parent JWT is verified by real local Auth; own-household operations use the existing KR-004 `accept_control` transaction through an invoker-rights HTTP RPC wrapper. UUID replay preserves the original actor/target/payload result. Expected versions protect LOCK/UNLOCK/SET_DAILY_LIMIT. ADD_TIME accepts only 600/1800 seconds and creates one immutable dated grant. Server transaction order/time is authoritative.

Credential-authenticated sync reads one consistent own-device snapshot: canonical version/policy, household zone/revision, trusted server UTC, current-period absolute bonus, and at most 100 ordered operation outcomes. `history_pruned` truthfully indicates omitted older outcome detail; the canonical snapshot still converges. This slice does not implement multipage history, delete historical command rows, or invent execution of omitted commands.

The child strictly validates JSON/schema/numbers/device/epoch, settles monotonic usage and commits the canonical policy and immutable pending ACK in the existing identity-bound Room ledger. Codec format 3 reads existing formats 1/2; Room schema remains 2 and migration 1→2 is unchanged. Duplicate/older policy cannot roll back version/usage. Existing A/B recovery remains authoritative. ACK retries preserve report sequence/body; server receipt time defines freshness. Parent status distinguishes accepted/pending/persisted/superseded/expired and rejected/failed outcomes. There is no enforcement adapter: `restriction_applied=false`, health `ENFORCEMENT_UNAVAILABLE`; persistence is never evidence of applied enforcement.

## Independent attempts

All JSON reports beside this file retain enumerated outcomes only; credentials, raw policy bodies, package history, logs and handoff contents are excluded.

| Attempt | OBSERVED result / follow-up |
| --- | --- |
| node-01 | 68/69 passed; existing static lock assertion expected old shared device lock. Updated to the required exclusive device lock. |
| node-02 | 82/82 affected Node tests passed. |
| db-http-01 | 542 SQL assertions and existing HTTP regressions passed before two final SQL assertions were added. |
| http-01 | Partial: incorrect test-mode keep flag stopped at development-ready before new HTTP tests. Targeted runner interruption cleaned owned services. |
| http-02 / http-03 | Failed changed-payload HTTP conflict. Instrumented follow-up observed no HTTP response, not a successful 409. |
| http-04 | Business-conflict wrapper worked; the new fixture then violated the existing one-owner uniqueness constraint (23505). Fixture corrected to a temporary owner swap and restored afterward. |
| http-05 | 544 SQL + 54 new real HTTP assertions passed, including 100 identical concurrent replays. |
| http-06 | 544 SQL + 59 new real HTTP assertions passed; added bounded 100-outcome window and cross-household status denial. |
| android-01 | Failed before instrumentation: retained uninstalled task packages were omitted by ordinary `pm list packages`; new CI signer caused `INSTALL_FAILED_UPDATE_INCOMPATIBLE`. Original cleanup was UNVERIFIED. Guarded follow-up found them with `-u` and uninstalled only the two task packages successfully. Harness now checks retained packages and records bounded install error codes. |

The HTTP conflict hang was the documented PostgREST 14 retry of custom SQLSTATE 40001. The narrow wrapper translates only the existing named business conflicts to PT409, retaining the original transaction and genuine serialization failures. See [Supabase official troubleshooting](https://supabase.com/docs/guides/troubleshooting/high-cpu-and-infinite-transaction-retries-when-using-custom-error-codes-in-rpc-functions-77326b). No dependency upgrade or conflict-validation relaxation was used.

## Validation checkpoint

**OBSERVED:** CI [34796218471](https://github.com/felipebarbosa4/KidRemote/actions/runs/34796218471) (`255babb`) and [34796653776](https://github.com/felipebarbosa4/KidRemote/actions/runs/34796653776) (`153871e`) passed all five required jobs. The latter includes 544 SQL assertions (521 existing + 23 new), 59 KR-009 loopback HTTP assertions, 100 identical replays, prior 28 pairing HTTP checks (gateway storage stub), 44 real Auth checks, 38 enrollment/removal HTTP checks and 27 rotation HTTP checks. Node suites total 126 tests across KR-003/004/005/006/007/008/009. Android JVM compilation/tests, parent/child build/lint/debug/release/instrumentation builds and release/security audits passed; spike checks remain isolated, without device execution.

**OBSERVED Android:** `android-02` passed 12 normal instrumentation methods plus one expected process death after Room commit before ACK. All host status/freshness checks passed; own app/test data and task backend resources were cleaned. APK/test source `153871ea74cc83cb224ac1b18a3370fec5ab489f`, CI merge checkout `f6dd4bb659ff7792e7a6cd4d43bbf21dd6d06d59`; exact package/version/signer/SHA-256 values are in `KR-009-LOCAL-2026-09-13-apks.json`. Host harness additionally contains the documented retained-package detection fix; it does not change the exercised APK. The first failed APK attempt used `255babb` and never reached instrumentation.

Shared Room regressions and final emulator shutdown remain pending at this checkpoint.

## Fixtures and claim boundaries

**OBSERVED:** Backend tests use actual isolated PostgreSQL transactions, local Auth confirmation/mail, PostgREST and gateway HTTP. The Android identity comes from actual redemption; no parent JWT is passed to the child. The child persists it using existing Keystore wrapping, then performs real sync/ACK HTTP and Room transactions. Monotonic eligibility/coverage is controlled instrumentation evidence, not measured physical UsageStats. Invalid/older snapshot tests mutate the real HTTP response at the child validation boundary. Lost-ACK tests withhold a real successful HTTP reply before local confirmation; server receipt comparison checks idempotency. Gateway outage stops the task gateway and later restarts it. The previous-day grant is an explicitly backdated SQL fixture, not a parent API capability or edited device clock.

**INFERRED:** These bounded tests support local convergence and durable retry for the exercised schema/fixtures. They do not prove arbitrary scheduling, multi-process Room use, physical power loss, physical usage accuracy or OEM lifecycle behavior.

**UNSPECIFIED / UNRUN:** FCM SDK/project/addressing, notification priority, provider retry/registration, dispatcher, background sync/backoff scheduling, production transport/deployment, physical p95 enforcement, UsageStats correctness, battery/OEM acceptance and capacity/latency acceptance. Push outbox remains the existing atomic KR-004 write with no provider dispatcher. No full KR-008, KR-003 or KR-009 acceptance is claimed.

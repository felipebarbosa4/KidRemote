# OD-50 product replacement preparation — BLOCKED

- **Goal:** One future immutable Windows runner: exact old provenance → authorized package-only destructive replacement → real enrollment/consent → canonical Lock/independent fixture/Unlock → separately recorded cleanup.
- **Context:** PR #24, baseline `45eda78b7c4d45f3ddb3971b134feea26d7e7f1f`. Owner explicitly chose option B for the dedicated Samsung tablet. OD-50 records this new authority without changing OD-49 or historical evidence.
- **Constraints:** No physical command in this task; no unrelated deletion/settings, key copying, FCM, screenshots, production hooks, merge or acceptance claims. Product telemetry cannot replace fixture input/focus.
- **Done when:** Complete validation permits one frozen command, or an evidenced blocker prevents publication and preserves all failed/partial attempts.

## Current verdict

**PRODUCT_PHYSICAL_ORACLE=BLOCKED. OWNER_BUNDLE=NOT_CREATED. PHYSICAL_EXECUTION=NOT_RUN.**

The candidate entrypoint throws `INVALID:OD50_LIVE_SUPERVISOR_VALIDATION_BLOCKED` before imports, ADB, backend or mutation. A manifest flag cannot bypass this source gate. No physical command is supplied. The owner-authorized replacement has **not** occurred.

## Implemented candidate boundaries

- Existing journal now admits PREMUTATION, uninstall, install, reverse, enrollment, setup, initial policy and cleanup stages, with flush-before-call and immutable verdict/separate cleanup. Replacement provenance pins the old/new certificates, versions and hashes. Existing/partial attempts cannot replay uninstall. Preparation returns PREPARED_NOT_PASS; only the independent oracle can determine PASS.
- Replacement commands fix the package to `dev.kidremote.child.unassigned.debug`; verify package absence after uninstall and exact installed signer/hash/version/service after install. No old-APK restore, clear-data, unrelated package operation or permission write exists. The existing read-only readers remain unchanged.
- Fixed reverse uses `--no-rebind` and readback; only a positively owned matching mapping is eligible for removal. An ambiguous reverse result remains UNVERIFIED, not falsely NOT_CREATED.
- Parent Auth/household and pairing use the existing real endpoints. One stable UUID/object is retried once for an ambiguous LOCK/UNLOCK response; auth denial/conflict/malformed responses do not change expected version. An explicit **3,600-second test fixture** configures an initially unconfigured policy; it is not a product default. No identity file injection or debug enforcement endpoint is added.
- Host-only QR generation uses existing ZXing and private pipes; planned QR display remains in memory. Owner performs camera/Usage Access/product consent/Accessibility actions. Settings are opened, never silently granted. The product's own consent button remains necessary.
- Live callbacks reuse ordinary-fixture broadcast replies, the approved bounded shell tap and counter/focus assertions. Real app resume requests sync; adapter/ACK/status only corroborate. Current source wires these callbacks but the complete native pipeline is **not validated**.
- The backend candidate reuses the current task-owned tmpfs/PostgreSQL/Auth/gateway tooling. An opt-in host-stdin STOP closes only its own session through existing cleanup. No new remote control route or production/release change. This disposable backend does **not** preserve a reusable server identity after cleanup: the retained child identity would be offline/orphaned when its synthetic backend is removed. That limitation must be resolved before promising the requested reusable normal test setup.

## OBSERVED local tests and failures, independently retained

1. **Real backend attempt 1:** native Docker Desktop via existing tooling started successfully. All **12 PostgreSQL suites / 574 assertions PASS** (RLS, constraints, operations/concurrency, pairing/races, bootstrap, rotation, sync/ACK/pages, parent presentation and enforcement observation). Separate existing pairing protocol adapter suite: 28 assertions, gateway storage STUB; do not relabel those 28 as live gateway evidence. Auth, REST, mail and gateway reached their loopback readiness checks.
2. **Real Windows parent Auth attempt 1:** `New-LabParent` failed; independent signup probe observed HTTP 400. Its generated test password was 76 characters. Corrected only the host generator to 68 characters; added a regression bound. No real password/token enters evidence.
3. **Real Windows parent Auth attempt 2:** signup/mail verification/household PASS. A separate real request sequence also passed new pairing-session creation and empty own-device list. This is real HTTP evidence; **no child redemption or Android enrollment was claimed**.
4. **Original backend cleanup:** graceful stop verified gateway shutdown, Auth services removed, exact owned DB removed and task network removed. No Samsung or emulator command occurred.
5. **Native Windows supervisor attempt:** `Start-ProductBackend` failed with `INVALID:START_DOCKER_DESKTOP_OR_REVIEW_BACKEND_PREREQUISITES` when exercising Windows→WSL orchestration from a task-owned source copy. Tested supervisor SHA-256: `1bd66b0b4f703b4a3a87c56bff7c726d762f1c5a474f292f729966e4017773ba`. Subsequently launching Windows PowerShell from this session failed with **Exec format error**; `/proc/sys/fs/binfmt_misc` lacked `WSLInterop`. The temporal sequence is OBSERVED; causation/root cause is **UNSPECIFIED**. No sudo, binfmt/WSL repair, host settings change or physical workaround was attempted. No task backend Node process remained in the Linux process inventory. A fresh Docker inventory for this failed supervisor could not be executed after interop loss; its external residual-resource state is **UNSPECIFIED**, not claimed verified clean.
6. **PowerShell synthetic preparation:** 75 checks PASS on native Windows before the interop failure and separately on portable Linux PowerShell 7.6.6. Covers wrong old/new provenance, backend/setup failure before uninstall, uninstall success/install failure, reverse/pairing/permission/policy failure, truncated admissions, immutable/restarted journals and no silent retry/replacement.
7. **Transport/retry suite:** first native invocation was NOT_RUN due Exec format error. Portable PowerShell first run FAILED at check 30 because a test's script-scope counter was not shared through a closure. Corrected only the fixture to use shared state; independent **36 checks PASS**, including fixed commands/rejections, reverse ownership/ambiguity and stable LOCK/UNLOCK response-loss retries. These are injected callbacks, not physical or live response-loss evidence.
8. **Existing regressions:** 72 prerequisite checks and 64 independent-oracle checks PASS. The latter preserve contradiction→FAIL, ambiguity→INVALID and independent restoration requirements. Node KR-006–009 tests **31 PASS**. Metadata shell tests **4 PASS**, host recovery parser tests **3 PASS**, all oracle PowerShell syntax parsers PASS. A portable test-only PowerShell download was used without installation or host configuration changes; it does not substitute for Windows native integration.
9. **Host QR:** Java 17 compile and **3 synthetic PNG/dimension/input-bound checks PASS**. Windows display, actual scanning and product pairing remain NOT_RUN. No QR secret or generated real QR file was persisted.
10. **Android/JVM/lint:** local `testDebugUnitTest lintDebug lintRelease` SUCCESS (12 seconds, 166 tasks, 6 executed/160 up-to-date); existing XML reports child 84 / parent 14, zero failures. No new Android instrumentation/emulator execution. Required CI adds native fake-ADB tests and an actual PostgreSQL/Auth/gateway stdin-stop regression; final results are reported in PR #24/Issues #3/#10 after completion.

Early read attempts guessed nonexistent legacy filenames and one Docker executable location; corrected using repository/file discovery. They were read/probe failures, never evidence of a device operation. Historical APKs, signing evidence, unknown-file evidence and prior physical verdicts remain unchanged.

## Remaining readiness gates

Complete Windows supervisor startup/ownership/shutdown needs a usable native validation environment. The full driver needs native end-to-end failure injection and a useful owned-emulator replacement/service/real product enrollment/sync run; current callback coverage cannot stand in for them. QR presentation and actual consent mechanics remain unverified. No immutable bundle or command is defensible yet.

The requested reusable enrolled setup also needs an explicit lab-backend lifetime solution: current cleanup deletes synthetic server state. Do not silently claim that leaving a local identity blob preserves a usable enrollment, or introduce a new persistent backend architecture in this preparation. No package uninstall may be admitted while these prerequisites remain unresolved.

Planned owner actions are five logical steps: initiate QR scan; grant camera permission on a fresh install; enable Usage Access; tap product consent; enable product Accessibility. Return launches are automated; no extra human visual-oracle prompt is planned. Exact physical tap count and duration are **UNSPECIFIED** until validated; current requested owner interactions are **zero** because no runner is published.

PASS requires independent positive input, blocked counter/focus with consistent product corroboration, canonical Unlock with positive allowance and independent restored input. Usable fixture input/focus under Lock is FAIL. Ambiguous transport, provenance, pairing or setup is INVALID. Canonical cleanup is separate from the original verdict; manual Accessibility-disable recovery never promotes a failed/invalid attempt to PASS. Hard host death may require that manual recovery through the product's safe Settings path; no physical recovery guarantee is claimed.

Approved lab unchanged: source `668ab87a22591319afd43167d55ef9ac0909c1b3`, versionCode 2 / `0.0.2-local-physical-lab`, SHA-256 `f6d2a240fae179343d9eb19dfde7684ae6e241b35cebea8ce491205110f7ad56`, signer `638dfa66379788415c313d7a3ca96dcfcaf7e643c12bb0c4950b3046a3f76beb`. No replacement APK, frozen bundle or physical qualification evidence was produced.

# KR-003 Q4 bounded recovery-repair handoff — 2026-09-06

Historical handoff: the owner executed this bundle and the [preserved result](KR-003-Q4-RECOVERY-2026-09-06.md) records a recovery-button physical
FAIL. The immutable bundle remains unchanged; the physical status below is its initial handoff state.

- **Goal:** Run one immutable calibration of the `NEW_TASK | CLEAR_TOP` Settings recovery candidate against the known-failed Digital Wellbeing route.
- **Context:** Q3 established phase-local ORDINARY_APP reattachment and physical recovery failure on the authorized Mi 8.
- **Constraints:** Owner-operated Windows ADB; diagnostic-only; no allowlist/identity collection, network mutation, 100 samples, destructive action or KR-004.
- **Done when:** Exact source/APKs/runner are verified and one owner command captures the four phases plus sample-preserving bailout.

Physical execution at initial handoff: **Not run**. It was subsequently executed with the outcome linked above.

## Immutable bundle

Source: `768aaa039ac4c774896f708591ac30d218405e39`. Windows directory:
`C:\platform-tools\kr003-qualification-bundles\768aaa0`.

| Artefact | SHA-256 |
| --- | --- |
| `bundle.json` | `8309c7405c660ef185e5d7aad93b39565afdcac36b13d2d58d9cdec1e26bf227` |
| `Start-KR003.ps1` | `4a510109ebcf5b8d7295aeecd72c9e4030b58ef2c7dee0a9c059b600f763afcc` |
| `Clear-KR003-Lab.ps1` | `9b940d171145ccd76a4f1695310aacee3e1c7604b3040e8460d318d5a859a8b3` |
| `Qualification.psm1` | `bb87b8386362acd60cd88672789a302c99e72c509c750e7e72a6139fb5e66799` |
| `protocol.md` | `5d039718a391b8953965b0ae5a94be3368940ecc46bf46fced5234e7bf05916e` |
| `candidate.apk` | `8d442b881c7febb2a8080790a82f7e8bffad1730d23f4afdaea8a67b946544b7` |
| `ordinary-fixture.apk` | `6653f10b527cc9a273a8c0ea045cd250f6978c1acfb8e92b00b701f6c14f84bb` |

All seven files were independently re-read after packaging. Every nested manifest hash matched, and all six payloads are byte-equal to their
source/build outputs. The bundle uses protocol `KR003-Q4-RECOVERY-REPAIR-CALIBRATION`, runner version 4 and `diagnosticOnly=true`. Earlier bundles
and physical run directories were not modified.

## Verification

[CI run 34057645951](https://github.com/felipebarbosa4/KidRemote/actions/runs/34057645951) passed all three jobs on the exact source:

- 24/24 Android JVM tests, debug/release lint and assembly;
- merged-manifest and release-DEX diagnostic isolation audit;
- eight Node evidence/security tests;
- 54 PowerShell state/oracle assertions and 83 finalization/runner assertions on Linux PowerShell;
- the 83 runner assertions on native Windows PowerShell 5.1;
- repository/link/least-privilege validation and branch whitespace checks.

Every CI device command was stubbed. Packaging independently reran the Android build, audit and repository validation. No physical result is
inferred. The candidate APK changed because only the Settings recovery intent flags changed; the ordinary fixture is unchanged.

## Exact owner command

Run once in owner-operated Windows PowerShell with only the authorized Mi 8 connected:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\768aaa0\Start-KR003.ps1" -RecoveryDiagnostic
```

Operator sequence:

1. Observe expiry remain continuous for ten seconds; report P/F/I.
2. Tap **Open device settings** once; verify top-level Settings responds; report P/F/I.
3. Tap **Digital Wellbeing & parental controls** exactly once. Report P if usable, F if the restriction returns, or I if uncertain.
4. If blocked, tap **Open device settings exactly once**. Do not tap it again. Watch top-level Settings continuously for at least ten seconds.
   Report P only if it remains usable, F if restriction returns/remains or recovery is unusable, or I if uncertain. The runner rejects an early P.
5. Wait for post-state capture and automatic sample-preserving lab CLEAR.

Estimated duration: **4–7 minutes**. A second recovery-button handler activation, physical F, missing safe transition, ordinary reattachment,
health/trace problem or unverified bailout prevents repair success. Exit 0 only means evidence/finalization completed. Every outcome has zero
qualification samples; do not explore another destination, press Home, alter permissions/network or rerun an attempt.

## Emergency lab-only bailout

If the runner is forcibly interrupted or the device remains trapped, open a second owner-operated PowerShell window:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\768aaa0\Clear-KR003-Lab.ps1"
```

This invokes only debug SNAPSHOT/CLEAR, verifies unarmed/unrestricted/detached state and requires the complete latency sample array to remain
unchanged. It does not clear app data, uninstall, alter permissions/network or count as consumer recovery evidence.

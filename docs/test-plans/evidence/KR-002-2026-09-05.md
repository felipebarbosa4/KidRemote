# KR-002 repository-foundation evidence — 2026-09-05

- **Goal:** Record objective completion evidence for the repository, agent map, documentation and CI foundation.
- **Context:** Architecture PR #11 was reviewed by automated checks and merged to `main` at commit `fbdc454e13a52671908d7e35cd3268308a7f3b59`.
- **Constraints:** Foundation validation does not certify product implementation or external platform behaviour.
- **Done when:** Every KR-002 acceptance criterion has observed repository or GitHub evidence.

## Acceptance evidence

| Criterion | Evidence |
| --- | --- |
| AC-1 | `AGENTS.md` maps README → bounded KR issue → spec/ADR → test/evidence and mandates Goal/Context/Constraints/Done when. |
| AC-2 | `node tools/validate.mjs` and `git diff --check` pass; validator checks links, issue contracts/dependencies, ADR shape, Issue Form, design tokens/assets and state fixtures. |
| AC-3 | `.github/workflows/planning.yml` uses pull-request and main-push triggers, `contents: read`, a full-SHA checkout pin and `persist-credentials: false`. PR run 33994293930 and main-push run 33994909988 passed. |
| AC-4 | `.github/ISSUE_TEMPLATE/implementation.yml` is present on `main`; the validator confirms required fields and unique IDs. |
| AC-5 | Unchosen application/toolchain versions remain **UNSPECIFIED**; only the verified checkout action pin is recorded in TOOLING. |
| AC-6 | `docs/github/PUBLISHED.md` records actual issues, Project values, UI-only gaps and explicit evidence limits. |

GitHub evidence:

- PR: https://github.com/felipebarbosa4/KidRemote/pull/11
- passing PR check: https://github.com/felipebarbosa4/KidRemote/actions/runs/33994293930
- passing `main` push check: https://github.com/felipebarbosa4/KidRemote/actions/runs/33994909988

## Failure and mutation boundaries

Both planning publishers have dry-run modes. The Project helper now refuses to create a missing Project implicitly and preserves
the existing exact-title Project target and existing live field values. An in-memory inconsistent state fixture was rejected by the validator.
No production secrets are present in examples or workflow configuration.
The initial architecture pass also observed the helper exit before mutation when the prior token lacked Project scope; commit `8202e23`
retains that truthful failure record in history.

## Unrun evidence

Physical-device testing is not applicable because no Android application exists at this checkpoint.
No Android build, backend integration, RLS, deployment, load or store-policy test was run for KR-002.

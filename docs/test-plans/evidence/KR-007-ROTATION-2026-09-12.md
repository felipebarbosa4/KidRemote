# KR-007 AC-6 local credential renewal — 2026-09-12

## Contract and scope

- Goal: actual local digest-only rotation and durable child response-loss/restart recovery.
- Context: OD-45 prospective AC-6 extension, source `36f85a4c4dbeac2b1e60798aa944334b8e1bdf9f`, draft PR #20 stacked on PR #19.
- Constraints: synthetic accounts, task-owned local database/gateway and explicitly verified emulator only. No physical operation, camera, enforcement, AC-7 removal, deployment or next issue.
- Done when: actual SQL/HTTP/Android renewal and restart evidence, regressions and cleanup are retained; otherwise report the precise unrun boundary.

The [wire contract](../../../packages/protocol/CONTRACT.md#kr-007-local-two-phase-credential-renewal-od-20--od-45-ac-6)
defines BEGIN/CONFIRM/STATUS, fixed five-minute old expiry, new 90-day expiry, day-30
online renewal and no expired/revoked revival. Both candidates and operation are
persisted before BEGIN in the existing encrypted identity. The gateway derives scope
from current records; database stores digests and lifecycle metadata, not credentials.
No configured-policy cache exists yet: this slice preserves identity through failure,
does not build policy synchronization and cannot claim a cached-policy runtime test.

## Executed local database and HTTP evidence

Command (WSL, native Windows Docker, no published DB port):

```sh
node tools/kr004/test-local-db.mjs '/mnt/c/Users/3feli/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe' 'npipe:////./pipe/dockerDesktopLinuxEngine' --enrollment-test
```

The owner-labelled, newly allocated tmpfs database identity was verified before any
migration; PostgreSQL 17.6. No shared resource/reset or remote database was used.

| Independent attempt | Observed result | Cleanup |
| --- | --- | --- |
| `036a4940-8a47-4df0-8a05-2de8f3487ea2` | All five migrations passed; first RLS inventory assertion failed because it still expected 15 tables, not 16. NOT_PASSED; not an authorization failure. | Task database/network removed, verified |
| `1aefb041-0179-478a-b7c1-740c44f41723` | 521 SQL assertions passed, including 25 rotation tests; Auth 44 and enrollment HTTP 29 passed. New rotation HTTP suite was not connected in this attempt. | Gateway/Auth/DB/network cleanup verified |
| `cdd9b72f-fdff-4e32-81b2-3d356c88cfc8` | 521 SQL assertions; Auth 44, enrollment HTTP 29, rotation HTTP 24 passed. Twenty concurrent identical BEGINs returned generation 2 and one fixed deadline; conflicting requests produced one winner, no extra generation/device. | Gateway/Auth/DB/network cleanup verified; secret log scan passed |

SQL includes actual roles/grants, trigger-injected rollback before commit with no
partial new credential/shortened expiry, due/expiry boundaries and pending revocation.
HTTP uses the actual database adapter/handler, real concurrent PostgreSQL sessions,
new authenticated reads, sibling/foreign/parent-route denial, response-discard/status
and repeated confirmation. Long-duration fixtures move synthetic row timestamps;
host/emulator clocks are not changed and production SQL has no test-clock input.
The existing pairing protocol suite (28 assertions) also ran; its gateway storage
stub remains explicitly labelled and is not used to claim rotation integration.

Initial focused Node run failed the now-supported route's obsolete 404 expectation
and the protected baseline suite hash after the table inventory increment. Corrected
expectations retain every original RLS assertion; the baseline hash test allows only
the explicit 15-to-16 table-count change. Subsequent focused run: **75 tests passed,
zero failed/skipped**. Repository validation and `git diff --check` passed.

## Android runtime and CI

Pending execution; SQL/HTTP success is not APK runtime evidence. The existing task AVD
was verified by owner manifest, exact AVD name and `ro.kernel.qemu=1`; API36 boot
completed. All Android operations explicitly target only that emulator. A separate
`apks-kr007-rotation` directory preserves previous exercised APKs.

The debug test hook withholds the real committed response **after HTTP receipt/JSON
parsing, before renewal consumes it**. It is not packet-level loss, fake server state
or authorization injection. The separate process-restart test verifies a changed PID,
same durable operation/candidate and actual authenticated read. No secrets or raw
exceptions are emitted. The hook's setter/storage must be absent from release DEX.

CI source run: [34677339010](https://github.com/felipebarbosa4/KidRemote/actions/runs/34677339010).
Completion and exercised APK identities will be recorded after actual execution.

## Limits preserved

All historical physical invalid-QR/readback and emulator NOT_PASSED evidence remains
unchanged. This does not establish physical Keystore/OEM backup/transfer, valid camera
pairing, enforcement, full Edge deployment, real use or all KR-007 acceptance. AC-2/3/4
remain partial; AC-7 broader removal remains outside this authorization.

## Current-worktree recovery — 2026-09-13

- Goal: finish the authorized AC-6 validation and synchronize the existing branch/PR.
- Context: recovered unchanged worktree at `36f85a4`; three modified files and this
  untracked evidence file were inspected before edits. Existing implementation includes
  migration/SQL tests, gateway adapter, encrypted child candidates and runtime tests.
- Constraints: preserve every historical attempt; task-owned emulator and synthetic
  local services only, no camera/readback/Samsung/enforcement/other issue.
- Done when: remaining real runtime, changed HTTP negatives, regressions and required
  CI have actual outcomes, cleanup is verified and bounded changes are pushed.

The pending diff added strict string validation for operation ID, three HTTP negatives
(array coercion, malformed candidate, unknown bearer), and gateway outage while BEGIN
is pending. No reset/stash/checkout or replacement of those edits was performed.

Last recoverable prior runtime checkpoint: AVD boot preparation only. No rotation
result report or rotation APK directory existed on recovery, despite the earlier
prospective directory statement above. Other historical result files/APKs remain.
No active backend or emulator was reachable; no interrupted AC-6 Android test is
established. Any unrecorded execution remains **UNSPECIFIED**, not PASS.

Prior CI `34677339010` completed: four jobs passed, Windows PowerShell failed at
`VisualCaptureWorker.Tests.ps1:57` removing a `.png.partial` file still held by a
process. The later PowerShell 7 step did not run. This historical failure is retained;
no physical capture occurred and no visual implementation changes are included.

Recovery regression: **83 Node tests passed, zero failed/skipped**, covering
KR-004/005/006/007. Repository validation and diff whitespace checks passed.

Independent recovery attempt `f5088573-afe0-41c5-ade4-c371efa51b1d`: five
migrations and **521 SQL assertions (25 rotation)** passed, plus pairing protocol 28
(storage stub labelled). Service `start` failed before Auth/HTTP/Android; overall
**NOT_PASSED**. Task containers/network removal verified. Read-only Windows inspection
then showed excluded TCP range `57350–57449`, covering the fixed lab ports, with no
listener on the four endpoints. Raw Docker failure detail remains UNSPECIFIED.

Move the fixed local-only debug/test endpoints to **47361/47362/47365/47366** to
avoid that reserved range; preserve loopback binding and release isolation. No host
port reservations, firewall, services or privileges are modified. This requires new
CI APKs; downloaded `36f85a4` APKs remain separately preserved and were not exercised.

The final runtime also adds actual child `expiredRetains` and `revokedRetains`
HTTP rejection/ciphertext-preservation checks using synthetic stored timestamps
and database revocation. Their execution remains pending the new CI artifacts.
The 15 focused KR-006/007 Node checks pass after these harness changes.

Independent port-corrected HTTP attempt `d367b2d1-18aa-46d1-9b81-7c78517851b9`:
five migrations, **521 SQL (25 rotation), 44 Auth, 29 enrollment HTTP and 27 rotation
HTTP assertions passed**. The 20-way BEGIN race and three new negative payload/auth
cases passed against PostgreSQL. Gateway/service secret scan and all task container/
network cleanup passed. Exit 0. Android is not claimed by this HTTP-only run.

## Exercised source and final Android attempt

Source `3202a5e2d5a02057de06ed12209fabc5f9660a76`,
[CI 34767486377](https://github.com/felipebarbosa4/KidRemote/actions/runs/34767486377):
all five required jobs passed, including native PowerShell 5.1/7, real SQL/HTTP,
Android JVM/build/lint and release DEX/manifest/backup-rule audits. Child JVM: 5 tests.
The prior failed Windows run is not reclassified. The CI spike job is unchanged
build/unit regression only, with no enforcement or device execution.

New APKs are kept in `apks-kr007-rotation-ports` inside the existing owned AVD
directory; previous APK directories remain unchanged. Exact runtime invocation:

```sh
KR006_RUNTIME_DIRECTORY=/mnt/c/Users/3feli/AppData/Local/KidRemote/kr006-runtime/e03b4820-193b-4132-b1fc-f7950eeed7fe \
KR007_ROTATION_RUNTIME=1 \
node tools/kr004/test-local-db.mjs '/mnt/c/Users/3feli/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe' 'npipe:////./pipe/dockerDesktopLinuxEngine' --enrollment-runtime
```

This command necessarily initializes a fresh synthetic backend and reruns its SQL/
HTTP setup/regressions before instrumentation. It does not enable camera/storage
mode. No camera or owner readback is repeated.

Final independent attempt `2af14f98-3cb8-412d-95aa-c63fc5ad3bf2`, exit 0:
**521 SQL (25 rotation), 44 Auth, 29 enrollment HTTP, 27 rotation HTTP**, and
**24 Android instrumentation invocations passed** on the verified API36/Android16 AVD.
Each Android stage returned host exit 0 and `OK (1 test)`. Fifteen enrollment/Auth
regressions and these nine AC-6 invocations passed:

| Stage | Actual observed result |
| --- | --- |
| normal | App contact rotates due credential, same device/epoch, new read accepted, old denied |
| loseBegin | Actual committed BEGIN response withheld before renewal consumption; encrypted pending candidates retained |
| outageRetains (pending) | Real stopped gateway; ciphertext unchanged |
| restartPending (BEGIN loss) | Different process PID, same persisted operation/candidate; authenticated new read; no extra generation |
| loseConfirm | Actual committed CONFIRM response withheld; local pending state retained |
| restartPending (CONFIRM loss) | Different process PID, same candidate promoted; authenticated new read; no extra generation |
| outageRetains (confirmed) | Real stopped gateway; ciphertext unchanged |
| expiredRetains | Actual app contact receives 401; activity launch preserves identity bytes |
| revokedRetains | Actual app contact receives 403; activity launch preserves identity bytes |

No duplicate device appeared during rotation. The long-duration boundaries use
synthetic stored timestamps; this is not 90 days of wall-clock endurance. SQL checks
new expiry exactly creation +90 days, before-day30 denial, fixed overlap, rollback and
old/new expiry/revocation. HTTP checks day30 due and recovery after old expiry.
Android executes within overlap and proves durable app persistence/restart; no
packet-loss, power-cut durability or physical/OEM claim is made.

Owned app/test data cleared; service secret scan passed; gateway/Auth/DB/network
removal verified; exact owned AVD stopped without deleting it. Windows read-only
listener check: zero on 5584/5585/47361/47362/47365/47366. No labelled task container
or network remains. Previous APKs/result files and both historical failed/partial
attempts above remain intact.

Local report: `results-2026-09-13T16-11-23-310Z.json` in the owned AVD directory.
Report SHA256: `7e0f2cd9ed5a072d5995a5c802a204278ff0387b83bfabe8590e8eb69bdc5b20`.

| Exercised APK (all source 3202a5e) | SHA256 |
| --- | --- |
| Parent debug | `9147751c9e8cd592074b09fc577f298f69cc6a1ec4f58ab84758f2cfd95a4411` |
| Parent instrumentation | `b9981b3ecfe84d511c2750dfc2add853da535a71886b590cd0d09a3c7dd92e75` |
| Child debug | `19281c00be60d4c09b19da577990354574620f279c9bd14f2925215aa8d9af4d` |
| Child instrumentation | `8458c7cb834eac0f47b0dfb6c0b564d71096ff6409b9f7f6939e90b4ce050d18` |

AC-6 is **demonstrated locally** under OD-45; Issue #7 stays open and PR #20 stays
draft/stacked on PR #19. AC-2/3/4 remain partial and AC-7 remains open. Configured
policy-cache preservation, full sync, physical storage/backup/transfer, valid camera
pairing, production Edge deployment and enforcement remain unrun/outside this slice.
No later issue, merge, deployment or real-family use is authorized.

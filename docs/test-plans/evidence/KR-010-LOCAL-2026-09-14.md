# KR-010 local parent experience — OD-48

Baseline `a3c6335e19d58a3b4ddcd04422bf9d70fbf96d29`, stacked branch `kr-010-local-parent-controls`, Issue #10. Local parent slice implemented and exercised; full physical acceptance remains open. No physical/Samsung/camera, enforcement, screenshots, FCM, deployment, distribution or merge.

## Independent attempts

- **OBSERVED build-01:** local affected JVM/Android compilation passed. Gradle used already accepted SDK licenses to install compile platform/build-tools 37; target/min APIs unchanged. This is compilation, not runtime.
- **OBSERVED node-01:** 29 tests across KR-006–009 Node suites passed.
- **OBSERVED http-01:** migration `202609140003_parent_presentation.sql` failed SQLSTATE 42601 before tests/HTTP. Initial diagnosis suspected the subquery alias `rows`; changed to `listed`, but that did not fix the failure. Task DB container/network cleanup verified. No HTTP result claimed.
- **OBSERVED http-02:** the same migration still failed 42601; inspection found the missing closing `$sql$` function delimiter. Added the delimiter. Both failed attempts and their verified task cleanup remain separate.
- **OBSERVED build-02:** lint failed `NewApi`: bounded error reader used `InputStream.readNBytes` (API 33) with min API 28. Replaced with a bounded standard stream loop; did not suppress lint or raise minimum API.

- **OBSERVED build-03:** lint analysis crashed internally (`No fir element was found for KtProperty`, ControlFaults.kt). Sources were edited while this build was running; interference is an inference, not an established tooling defect. No lint suppression added; next build uses stable sources.

Logs retained independently in task-private `/tmp/kr010-{build,node,http}-NN.log`. Later runtime/APK/CI/cleanup checkpoints are recorded below, without changing earlier outcomes. Earlier evidence remains unchanged.

- **OBSERVED http-03:** 558 SQL assertions (550 prior + 8 presentation), 84 KR-009 HTTP and previous Auth/enrollment/rotation suites passed; task cleanup verified. New KR-010 HTTP suite was added after this run loaded code, so its checks are not attributed to this attempt.
- **OBSERVED build-04:** stable-source full parent/child JVM/debug/release/instrumentation build and lint passed, source `38341b4`. Parent 14 JVM, child 68 JVM; 97 KR-004–009 Node tests passed independently.
- **OBSERVED audit-01:** parent audit passed, child audit could not locate aapt2 because ANDROID_HOME was omitted. **audit-02** with explicit installed SDK path passed; no audit bypass.
- **OBSERVED android-01:** partial run at source `38341b4`, exact hashes in its JSON. Real Auth/empty/QR/enrollment, +600/+1800 and lost-response retry, real Room/ACK, lock/zero/unlock, superseded and conflict passed. The invalid-limit stage failed when its input was disabled: after process restart, a previously rejected request was restored as uncertain (only request, not admission result, had been saved). Corrected durable admission-result persistence, preserving UUID and ambiguous-write recovery. This partial is NOT_PASSED and not combined with a later success. Actual task app/backend cleanup verified. This attempt also passed 10 new real JWT/RLS presentation HTTP checks.

- **OBSERVED build-05 / audit-03:** full build/lint/security passed at `36d4b39`, 14 parent and 68 child JVM; Node regression repeat 97/97.
- **OBSERVED android-02:** rejected-result restart/invalid-limit regression passed; actual backend outage retained report; stale state and health presentation fixtures passed. Preliminary warm check 555 ms asserted semantic existence, not visible useful rendering, so it is not AC-7 acceptance. The 2x font stage failed an assertion; the initial redacted line points only at the helper, so root cause is not established yet. Added enum-only action/layout diagnostics; no screenshot/tree dump or loosened assertion. Task app/backend cleanup verified; font restored by finally. Original partial remains NOT_PASSED.
- **OBSERVED APK identity inspection attempts:** first parser expected older apksigner `Signer #1` wording; current output says `V2 Signer`. Second parser rejected absent test-package version metadata. Both attempts failed before recording identity; third inspection verified signer/package and records test versions as UNSPECIFIED, not invented values.

- **OBSERVED build-06 / audit-04:** full build/lint/JVM/security passed at `5cc3d43`.
- **OBSERVED android-03:** moved 2x font check earlier (host-only ordering change, same source APK). Four normal stages passed, then `ACTION_CHECK_0`, `ACTION_LAYOUT_WIDTH` identified text-width overflow in +10's intrinsic label layout. Expanded the actual button label to available width with centered text; retained the same no-overflow/48 dp assertions, no font-size reduction or test suppression. Exact source/APK and failed row retained; task app/backend cleanup and font restoration completed.

- **OBSERVED build-07 / audit-05:** full build/lint/JVM/security passed at `321c84a`.
- **OBSERVED android-04:** actual 2x critical-action labels all passed no-overflow and minimum 48 dp checks. Extra intentional +600 while manual lock remained set produced absolute bonus 3000, retained used=3600000, and retained manual lock. Warm list visible-render check observed 382 ms (one local synthetic sample, not performance acceptance). Final logout reached cleared model/vault assertions but the ambiguous text-only `Entrar` selector failed (title and button share text). Fixed by selecting the actionable login button; no application behavior changed for this fix. Original partial and task cleanup retained independently.
- **OBSERVED build-08:** test-only focus-order assertions and corrected logout selector compile/lint/JVM passed at `6216ce3`; application behavior unchanged from `321c84a`.

- **OBSERVED android-05:** all five 2x label bounds passed again, but the new keyboard-focus assertion failed when requesting button focus while the emulator remained in touch mode. Next test explicitly enters keyboard mode through Instrumentation before traversal; no production focus behavior changed. A focused actual-Action component fixture is added to diagnose this without repeatedly recreating backend state; it is not backend/product-flow evidence. The failed flow (four prior stages PASS) remains separate with verified app/backend cleanup.
- **OBSERVED source metadata correction:** initial build-08 copy metadata had a manually transcribed incorrect full SHA despite a correct seven-character prefix. Verified against `git rev-parse`, retained `source-invalid.json` separately, and corrected to `6216ce382c739c2d70ebe9ecc9dc0d906a177d95` before any runtime. APK bytes/hashes were unchanged; no run is attributed to the invalid SHA.

- **OBSERVED build-09 / audit-06:** final exercised application/test source `6a316e585ac49969f3e87c0c61ecc1d662927473` passed full parent/child debug/release/instrumentation builds, lint and security/privacy audits. Parent application bytes are identical to `321c84a`; subsequent application-code changes did not occur. Parent JVM 14, child JVM 68; affected Node regression repeat 97/97.
- **OBSERVED focused layout attempts:** layout-01 began before the APK metadata copy completed and stopped with FileNotFoundError before any Android action. layout-02 used the completed source bundle and passed all five 2x labels/48 dp targets and keyboard traversal +10→+30→Lock after entering keyboard mode. This is actual-Action component evidence, not backend flow evidence. Parent/test data cleared and font restored.
- **OBSERVED android-06:** complete actual Compose/Auth/PostgreSQL/gateway/Room/ACK flow **36 instrumented stage executions PASS** (parameterized step invocations, not 36 distinct Kotlin methods). It includes the keyboard/2x checks, real +600/+1800, lost-response restart/retry, an additional +600 while manual lock persists, zero/Unlock/both reasons, stale/conflict/superseded, explicit invalid limit, actual REST outage, minimal health fixtures, and logout with unchanged child aggregates. Final bonus=3000 seconds and used=3600000 ms. Warm useful list was visibly rendered after refresh in **345 ms**, one synthetic owned-emulator observation; not a p95/backend/production acceptance result. App/test and backend cleanup verified.
- **OBSERVED Auth regression:** **four existing KR-006 instrumentation methods PASS** on the same APK source, including process restoration, actual vault/key logout clearing, real email password recovery, old-password denial and REST outage/recovery. Task parent data/backend cleaned. Historical KR-006 evidence unchanged.

## Implemented local workflow and results

MY DEVICES and detail read actual own-device metadata, canonical edit preconditions and latest ACK through one invoker/RLS SQL statement (50 rows/page; existing tables only). Nickname/model, remaining, server receipt timestamp, manual/expired reasons and health are labelled as reported values. There is no parent countdown. Current server period/version supplies operation preconditions; reported remaining/manual state changes only after the child report.

Existing sessions open their device list; initial household timezone confirmation stays explicit. One latest operation request and its known admission result are Keystore-wrapped under noBackup and account/epoch bound. A new tap creates a UUID; retry/restart retains its original payload/ID. Unknown transport/write outcomes retain retry intent; known rejection survives restart. Same-period used time and bonus are never reset by parent limit changes. Existing QR create/cancel/expiry/navigation handling is reused, with no camera execution. Child changes only present existing local manual/expired/uncertain reasons in a scrolling light UI and continue to state that enforcement is unavailable.

| Requested case | OBSERVED result / boundary |
| --- | --- |
| 1–3 | Real signup/email verification/login → empty list → existing QR/decoder enrollment → real ACK displayed in detail. |
| 4–6, 12 | From zero, +10 yielded 600000 remaining ms; +30 yielded 2400000. A real committed +30 HTTP reply was withheld at the app result boundary; process restart and the Retry button reused its UUID, leaving one 1800-second grant. Existing HTTP suite separately replays an operation 100 times. |
| 7–9 | Positive+LOCK persisted manual=true while remaining stayed positive; later limit=0 retained usage/bonus and displayed both reasons. UNLOCK left zero/expired. A further +600 while manual=true retained the manual reason. Nothing rendered enforcement applied. |
| 10–11 | Actual REST container outage retained timestamped last-known data and retry. A one-hour-aged receipt fixture rendered stale/offline, never Online; this age fixture is not a measured radio outage. |
| 13–14 | An own-actor concurrent SQL operation superseded the displayed request after real child ACK; a stale expected-version UNLOCK showed conflict. -1/86401/1.5 limit inputs exposed explicit validation in Compose semantics without sending a new operation. |
| 15 | Actual logout cleared parent session/request state; same backend child's used_ms and absolute bonus were unchanged before harness cleanup. |
| Accessibility | Real Compose action labels, all five 2x no-overflow/≥48 dp checks, and keyboard focus +10→+30→Lock passed. Isolated update/permission rendering fixtures passed; no screenshot/tree dump or physical TalkBack claim. |

**Totals for successful Android evidence:** 36 real-flow stage executions + four Auth methods + one isolated component test = **41 PASS executions**. Prior partials are independent, excluded from that total, and not pooled into a pass. No Room migration/death/physical enforcement experiment was added; KR-008 core and A/B implementation were reused unchanged.

**SQL/HTTP/JVM/Node:** 558 SQL assertions including concurrency/RLS (550 existing + eight new); 84 KR-009 loopback HTTP checks including 100 replays; ten new actual JWT/RLS presentation HTTP checks. Earlier HTTP suites retain 28 pairing (gateway storage stub, SQL real), 44 real Auth, 38 enrollment/removal and 27 rotation. Parent 14 JVM and child 68 JVM passed. 97 affected KR-004–009 Node tests passed; required CI also runs 29 planning/ingestion Node tests (126 total). Build/lint/debug/release and least-privilege/secret/privacy audits passed. No parent/child package history is collected.

**CI checkpoints:** runs 34804055784 (`38341b4`), 34804530997 (`36d4b39`), 34805027630 (`5cc3d43`), 34805524031 (`321c84a`) and 34805997580 (`6a316e5`) each passed all five jobs. The final reporting-head CI is linked in Issue #10 / PR #23 after completion; its APK is not substituted for the locally exercised artifact.

## AC status and stopping boundary

- **AC-1:** local list/detail/loading/empty/reported/stale/error/degraded presentation demonstrated. Permission/update flags are component fixtures; actual permission/compatibility producers and healthy enforcement remain dependent on later authorized integration.
- **AC-2 / AC-3:** locally demonstrated: UUID reuse/new intent, truthful admission/persistence, independent manual/expired reasons, retained used/bonus and explicit limit conflicts. No applied-enforcement result fabricated.
- **AC-4:** reused local Auth/QR generation/cancel/fresh-QR/enrollment paths exercised. Historical camera/expiry evidence untouched; no new camera/physical acceptance.
- **AC-5:** Not run: no enforcement adapter or physical blocked-screen/safe-recovery acceptance.
- **AC-6:** partial: local semantics/2x actions/48 dp/limited keyboard traversal pass. Physical TalkBack, full focus traversal, 200% phone/tablet/OEM acceptance remain unrun.
- **AC-7:** partial: one visible local warm refresh at 345 ms plus actual backend-failure retention. No production/backend load or physical ≤1.5 s acceptance inferred.
- **AC-8:** Not run: no application screenshots/store assets/distribution authorized.

**INFERRED:** unchanged canonical operation/accounting paths plus observed restart/ACK behavior support this bounded local parent workflow. **UNSPECIFIED / UNRUN:** physical enforcement/UsageStats accuracy, safe p95/latency, battery/OEM/background acceptance, FCM/provider delivery, real permission/update health producers, production Auth configuration and real-family/store use. KR-003/008/009 and the full KR-010 issue remain open. No merge or next issue.

## Exact exercised artifacts and cleanup

Final runtime source: `6a316e585ac49969f3e87c0c61ecc1d662927473`, local Gradle build-09 with the existing pins. Parent package `dev.kidremote.parent.unassigned.debug` v1; child `dev.kidremote.child.unassigned.debug` v2. Test-package version metadata is absent and recorded as UNSPECIFIED. Signer SHA-256 `638dfa66379788415c313d7a3ca96dcfcaf7e643c12bb0c4950b3046a3f76beb`.

- Parent APK SHA-256: `514b4fd43d9caadaeacaaed3cbdba501733b9b415bc35d588f60719197a739e4`.
- Parent test APK: `82b08632f152ebf87bc910c0925a29d8d4641bdecfd60fe410402fd625461ece`.
- Child APK: `7502d0ea78d013e6c97c76c22af293aec1f3c3e6278edaa77ae9a1772ee9197b`.
- Child test APK: `5cc670ca2ed86996ff0e870d9c4c0a4517fc0659f25eac1815e9df8340a804fb`.

All earlier bundles remain separate under the same owner directory. The exact owned AVD `kr006_e03b4820193b4132b1fcf7950eeed7fe`, emulator-5584/API 36, was used. Successful harnesses cleared their task package data and removed backend containers/network; final label-filtered Docker queries returned no resources. The owner helper stopped emulator PID 31828; subsequent check verified the process absent and ports 5584/5585 not listening. Font scale was restored by finally. AVD/APKs/evidence retained; no global ADB shutdown or physical/Samsung operation occurred.

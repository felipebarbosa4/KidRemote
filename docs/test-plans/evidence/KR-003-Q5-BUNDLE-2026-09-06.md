# KR-003 Q5 Settings task-reset handoff — 2026-09-06

- **Goal:** Run one immutable physical calibration of the final flag-only `NEW_TASK | CLEAR_TASK` recovery candidate against the exact Q4 failed route.
- **Context:** Q4 reached Settings transiently, returned to `ORDINARY_APP` after 681 ms and physically failed recovery on the authorized Mi 8.
- **Constraints:** Owner-operated Windows ADB; diagnostic-only; no allowlist/identity collection, network mutation, qualification, destructive action or KR-004.
- **Done when:** Exact source/APKs/runner are verified and one owner command captures the four phases plus sample-preserving bailout.

Physical execution: **Not run**. This is an immutable candidate handoff, not physical success, qualification, production approval or Play acceptance.

## Immutable bundle

Source: `97173d207c8076219c6c4c8d780db43d8f9fc566`. Windows directory:
`C:\platform-tools\kr003-qualification-bundles\97173d2`.

| Artefact | SHA-256 |
| --- | --- |
| `bundle.json` | `89a8bc196f79cd655b4b6e7f83a64d0aae4b2582158f55a376d69381a4276ab1` |
| `Start-KR003.ps1` | `8bc42c58999676806bf731d13416de1430bfeb87ec5e0c0073132a4ec6a7e5f7` |
| `Clear-KR003-Lab.ps1` | `9b940d171145ccd76a4f1695310aacee3e1c7604b3040e8460d318d5a859a8b3` |
| `Qualification.psm1` | `e7f7c0453d5c0eb1316d32ffd049282fbbaacb9689e2198b5b9ad7708b82f63d` |
| `protocol.md` | `af7b3f0cb64d9c544c9e9d4cc801c735155363234e204af467dce28ab13e3861` |
| `candidate.apk` | `5b27c891fe155ee4d26e4da68f8323f178f7116e73d8097ce07199e5800e318b` |
| `ordinary-fixture.apk` | `6653f10b527cc9a273a8c0ea045cd250f6978c1acfb8e92b00b701f6c14f84bb` |

All seven files were independently re-read after packaging. Every nested manifest hash matched, and all six payloads are byte-identical to their
committed source/build outputs. The bundle uses protocol `KR003-Q5-RECOVERY-TASK-RESET-CALIBRATION`, runner version 5 and
`diagnosticOnly=true`. Earlier bundles and physical run directories were not modified.

## Change and verification

The same `ACTION_SETTINGS` recovery action now uses exactly `NEW_TASK | CLEAR_TASK`; `CLEAR_TOP` and `MULTIPLE_TASK` are absent. The phase reducer
records safe-then-ordinary overlay reattachment as `RECOVERY_REGRESSED_TO_ORDINARY`, and the pure verdict understands that blocking the labelled
Digital Wellbeing route is the precondition rather than an overall repair failure. See [the complete decision](../KR-003-RECOVERY-TASK-RESET.md).

[CI run 34059150290](https://github.com/felipebarbosa4/KidRemote/actions/runs/34059150290) passed all three jobs on the exact source:

- 24/24 Android JVM tests; debug/release lint and assembly;
- all four merged-manifest and release-DEX isolation audits;
- eight Node evidence/security tests;
- 61 PowerShell state/oracle assertions and 83 finalization/runner assertions on Linux;
- the 83 runner assertions on native Windows PowerShell 5.1;
- repository/link/least-privilege validation and whitespace checks.

Every CI device call was stubbed. Packaging independently reran the Android build, audit and repository validation. None of this is physical Q5 evidence.

## Exact owner command

Run once in owner-operated Windows PowerShell with only the authorized Mi 8 connected:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\97173d2\Start-KR003.ps1" -RecoveryDiagnostic
```

Operator sequence:

1. Observe expiry remain continuous for ten seconds; report P/F/I.
2. Tap **Open device settings** once; verify top-level Settings responds; report P/F/I.
3. Tap **Digital Wellbeing & parental controls** exactly once. Report F when restriction returns, or I if uncertain. Unexpected ordinary use is not success.
4. Tap the overlay **Open device settings** button exactly once. Do not tap it again. Watch top-level Settings continuously for at least ten seconds.
   Report P only if it remains usable, F if it flashes/returns to restriction/remains unavailable, or I if uncertain. The runner rejects early P.
5. Wait for post-state capture and automatic sample-preserving lab CLEAR.

Estimated duration: **4–7 minutes**. A physical F, transient Settings, ordinary reattachment, multiple dispatch, missing required transition,
trace/health issue or unverified bailout prevents success. Exit 0 means evidence/finalization completed only. Every outcome has zero qualification
samples. Do not explore another destination, press Home, alter permissions/network or retry a prompt.

If Q5 fails this route, stop flag-only recovery experiments and return KR-003 consumer recovery to architecture/go-no-go review. Do not rerun Q5
to replace a failure.

## Emergency lab-only bailout

If the runner is forcibly interrupted or the device remains trapped, open a second owner-operated PowerShell window:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\97173d2\Clear-KR003-Lab.ps1"
```

This invokes only debug SNAPSHOT/CLEAR, verifies unarmed/unrestricted/detached state and requires the complete latency sample array to remain
unchanged. It does not clear app data, uninstall, alter permissions/network or count as consumer recovery evidence.

# KR-008 local accounting — 2026-09-13

- **Goal:** Validate bounded local accounting under explicit OD-46 authorization.
- **Context:** Branch `kr-008-local-accounting`, stacked on KR-007 `4537954bf6a8811d9dc37a4599e2f46526f16441`; Issue #8 remains open.
- **Constraints:** No physical execution, enforcement acceptance, deployment, family data, KR-009 sync or historical KR-007 evidence changes.
- **Done when:** Local results and failed attempts are recorded honestly; required CI passes; remaining AC boundaries stay explicit.

## Checkpoint before Android build

**OBSERVED:** focused KR-006/007/008 Node tests 19/19 pass; `node tools/validate.mjs` and `git diff --check` pass. Added 28 deterministic accounting tests (33 child tests total) and 10 ordered instrumentation stages, including two deliberate process kills. They are not yet executed at this checkpoint.

**INFERRED:** code isolates epoch-bound aggregate persistence and uncovered-suffix reconciliation without a network policy system. The [lab README](../../../tools/kr008/README.md) documents interfaces and conservative boundaries.

**UNSPECIFIED:** build/unit/runtime results until executed; physical p95, UsageStats correctness, battery, OEM lifecycle, app-update acceptance and clock recovery. KR-009 owns sync/receipts. No AC is closed from code inspection alone.

## Attempts and final results

The initial pending checkpoint is preserved below; final source/runtime results follow.

**OBSERVED checkpoint:** [CI 34773376557](https://github.com/felipebarbosa4/KidRemote/actions/runs/34773376557) passed all five jobs for `3f873a3`. Child JVM: 33 tests (28 accounting + 5 enrollment); Android builds/lint and APK privacy audits passed. Runtime has not run against this checkpoint. Subsequent review adds a pre-integration recovery-clock guard and a regression (34 child tests), plus an explicit persisted-manual-lock restart assertion; the next APK will carry those changes.

**OBSERVED attempt 1:** [sanitized report](KR-008-LOCAL-2026-09-13-attempt-01.json), APK source `3f873a3`, host runner `eea8d6a`, owned API-36 AVD only. Eight normal stages PASS and two EXPECTED_PROCESS_DEATH stages, each with a durable marker and matching instrumentation crash. Real SQLite migration, refusal of incompatible downgrade, before-commit rollback, after-commit survival, write-trigger failure, corruption, concurrent suffix and identity expiry/removal boundary passed. Synthetic app data cleanup verified. No unexpected failed or interrupted runtime attempt occurred. The deliberately killed processes remain individually recorded and are not counted as ordinary passing JUnit invocations.

## Demonstrated boundary by acceptance criterion

| AC | Classification and local result | Still required / not claimed |
| --- | --- | --- |
| 1 | **OBSERVED locally:** deterministic monotonic fixtures count eligible use once and exclude off/keyguard/blocked/sleep; unknown sleep coverage degrades without charging it | Physical signal/UsageStats correctness; connected-service lifecycle |
| 2 | **OBSERVED partial:** real Room stores used time/version/absolute bonus/manual lock/cursor; separate process and killed-transaction recovery preserve committed state; v1→v2 fixture migration and incompatible downgrade refusal pass | Pending receipts (KR-009), real app-update acceptance, adapter/ack seams |
| 3 | **OBSERVED locally:** uncovered suffix only; reordered/duplicate ranges, conflict/gap/truncation fixtures and two concurrent real database writers | Physical retained-history completeness and accepted recovery error |
| 4 | **UNSPECIFIED / UNRUN:** desired restriction is derived, never reported as applied | Physical offline p95 ≤2 s enforcement and approved emergency/recovery surfaces |
| 5 | **OBSERVED domain slice:** controlled boot changes retain balance/date and mark CLOCK; monotonic regression is rejected; controlled ±24 h RTC values/default-device-zone edits cannot affect the trusted-anchor projection | Actual offline reboot/RTC editing on physical Android, OEM boot reliability and trusted re-anchor/recovery |
| 6 | **OBSERVED locally:** exact/split midnight, Toronto 23/25-hour DST dates, retained used time after limit changes, manual lock across midnight, absolute bonus and stale versions | Physical integration remains unverified |
| 7 | **OBSERVED partial:** missing/truncated/conflicting coverage, missing DB, actual SQLite write failure and corrupt record preserve known use or return unavailable/degraded; no new allowance from restart/reinitialization | Approved recovery flow and trusted clock/history restoration, physical error tolerance |
| 8 | **OBSERVED privacy slice:** aggregate-only codec/Room schema, no package getters/history/network in accounting code; debug probe is transient and cannot certify coverage; APK audits exclude probe/Usage Access from release | Battery/checkpoint cost and accepted physical recovery error are **UNSPECIFIED** |

No full Issue #8 checkbox is closed by the local-only exception. AC-1/3/5/6 have a local core demonstration; AC-2/7/8 remain explicitly partial and AC-4 is unrun. This does not close KR-003 or qualify any OEM/device. There is no autonomous service or adapter integration and no configured-policy network evidence.

## Regression interpretation and privacy

**OBSERVED:** required CI executes 116 Node tests across KR-003–008, 521 SQL assertions (243 RLS, 45 constraints, 57 atomic control, 44 concurrency, 62 pairing, 36 pairing races, 9 parent bootstrap, 25 rotation), 28 pairing protocol HTTP assertions with gateway-storage stub, 44 real Auth/PostgREST, 38 real enrollment/removal HTTP/DB and 27 real rotation HTTP/DB assertions (including 20 concurrent identical requests). These are unchanged backend regressions required by CI, not accounting network evidence. Android parent/child and isolated spike unit/build/lint, packaged backup/permission/release isolation and no-device PowerShell jobs pass at the recorded source checkpoints.

**INFERRED from implementation and audits:** accounting stores only the active policy fields, current/previous aggregate and bounded clock/coverage checkpoint; no package/app/content timelines or separate identity credential. Evidence stores only enumerated results, source IDs and APK hashes. Android runtime uses synthetic identity and canonical local fixtures, real SQLite/Room/Keystore and controlled accounting clocks. The OS clock/signal sampling and conservative no-coverage UsageStats probe execute on the owned emulator; actual usage measurement correctness is not established.

**UNSPECIFIED:** real-family behavior, physical enforcement, battery, OEM lifecycle and trusted recovery. KR-009 must supply canonical policy/time and convergence/receipts later; this PR does not implement them. Historical KR-007 evidence, PR #20 and its reporting head remain unchanged.


## Final code and Android checkpoint

**OBSERVED:** [CI 34773744527](https://github.com/felipebarbosa4/KidRemote/actions/runs/34773744527) passed all five jobs for final code `eea8d6a71ef4f3909c5bb5464246ce8f770d2c84`. All 34 child JVM tests pass (29 accounting + 5 enrollment), including recovery-clock rejection before midnight advancement. Build/lint/debug/release isolation and required regressions pass with the counts above.

**OBSERVED attempt 2:** [sanitized report](KR-008-LOCAL-2026-09-13-attempt-02.json), matching `eea8d6a` APK and runner. All eight normal stages PASS; both deliberate kills meet EXPECTED_PROCESS_DEATH criteria and their subsequent recovery assertions pass. Manual lock is now explicitly stored before a different-process restart and verified before a newer local fixture unlocks it. No unexpected failure, hidden rerun or interrupted test remains. The full ordered suite was rerun because recovery-clock handling and the persisted-lock assertion changed; the earlier attempt remains separate.

The debug APK SHA-256 `83c33ddcb596b8843802ed5b11213ab5195bd0c24e768608f8c403a347f033e9` matches CI. Both APK pairs remain in the task directory (`apks-kr008-accounting-ci1-3f873a3` and `apks-kr008-accounting`). Both runtime JSON reports remain separately preserved there and in this repository. Task app/test data cleanup was verified after each attempt. No camera/readback, physical ADB, enforcement or network accounting scenario was run.

PR [#21](https://github.com/felipebarbosa4/KidRemote/pull/21) remains draft/open, targeting `kr-007-local-enrollment`; [Issue #8](https://github.com/felipebarbosa4/KidRemote/issues/8) remains open. The final reporting commit changes documentation/evidence only; its required CI status is tracked on the PR without claiming its separately rebuilt APK was exercised.

**OBSERVED cleanup follow-up (preserved failed check):** owned-emulator stop returned `TASK_STOP_REQUESTED_NO_AVD_DELETION` and its launch session exited 0. The first post-stop assertion was too broad: it rejected any TCP row at ports 5584/5585 and returned `OWN_EMULATOR_PORTS_STILL_OPEN`. Inspection found only two `TimeWait` rows with owner PID 0, not a listener/running emulator. A corrected read-only check verified original process 10616 absent and no active/listening connection; it passed with `OWN_EMULATOR_PROCESS_STOPPED_NO_LISTENER_ONLY_TIME_WAIT`. No further device mutation was used. This failed cleanup assertion is separate from the passing instrumentation attempts and is not silently erased.

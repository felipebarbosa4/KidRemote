# KR-004 local database AC-1–4 execution — 2026-09-11

- **Goal:** execute the OD-42-authorized local schema/RLS slice, not a deployment.
- **Context:** resumed clean `8fe80eb0ea0936662af86996f674ab68964d32c5` on
  `kr-004-local-schema-rls`; existing draft PR #17 remains stacked on
  `kr-003-mi8-overlay-stability`. PR #16 is not merged or rewritten.
- **Constraints:** synthetic identities, task-owned local container only; no host integration
  repair, TCP exposure, shared reset, real accounts, ADB, capture or later-issue work.
- **Done when:** migrations and actual role-context assertions pass, scoped cleanup verifies,
  local checks and CI are recorded, AC-5–7 remain open.

## OBSERVED

The native Windows Docker client with explicit local
`--host npipe:////./pipe/dockerDesktopLinuxEngine info --format '{{.OSType}}'`
returned `linux`, exit 0. This independently resolves the previous agent runtime blocker.

Actual successful database command:

```sh
node tools/kr004/test-local-db.mjs "/mnt/c/Users/3feli/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe" "npipe:////./pipe/dockerDesktopLinuxEngine"
```

| Verified field | Retained value |
| --- | --- |
| Container ID | `bb175406e668f9dd1044475314824ab360a8c77fb4f3b422370ce821cf2973ca` |
| Container name | `kr004-ac14-a1ab5be3-7b87-49f6-b58f-db1e797061e7` |
| Ownership label | `org.kidremote.kr004.disposable=a1ab5be3-7b87-49f6-b58f-db1e797061e7` |
| Image | `supabase/postgres:17.6.1.136@sha256:f371b5f3f2ac0a05703f33d6e6134515fb2498cab708fb948a0aeb7481467c00` |
| Engine / database | Local Docker Desktop Linux / `postgres` in that new exclusive instance |
| Before migrations | `postgres:supabase_admin`; zero public/private application tables; existing Supabase `auth.uid()` |
| Network / data | `none`; zero published ports; task-only tmpfs; no shared volume |
| Executed PostgreSQL | `17.6`; pgTAP available from image |
| Migration | `202609110001_schema_rls.sql` PASS |
| RLS suite | `01_rls.test.sql`: **243 PASS**, 0 FAIL, 0 SKIP |
| Constraints suite | `02_constraints.test.sql`: **45 PASS**, 0 FAIL, 0 SKIP |
| Finalization | Exact ownership reverified; only this container removed; successful filtered listing confirmed absence; exit 0 |

This intentionally removes only disposable synthetic DB contents. Sanitized execution
evidence is retained here; no secret or raw database/engine dump is committed.

### Executed input hashes (SHA-256)

| File | SHA-256 |
| --- | --- |
| Migration | `8865eb717c5fd7057bb0b0c0cf268f72e385e36bba8d5733c13079d230a4f781` |
| RLS suite | `8fe4df55823176ba18277b1338b8b67ee49d3574fe4f10d4364f37d82178b42c` |
| Constraints suite | `8fb621c0b0336c3415f70567a87460aaf467a4d95d2a6f5fc55c16cf3d771d64` |
| Runner | `44682f0b14505088fb37d62890cb584f1247805b2a21b700b6e577692abbec68` |

### Development failures retained, not counted as PASS

Earlier fresh task containers were independently removed with verified cleanup:
`1543153be659` (missing process environment password forwarding; init timeout),
`b331402f4ca2` (temporary init server seen as ready; later connection exit 2),
`dd6b7caff446`, `12a012339888`, `5876a2f95aba` (SQLSTATE `0A000`: invalid nested
data-modifying CTE in the test; fixed by placing it at statement top level),
`0b4ac6adbbdb` (private function EXECUTE assertion failed),
`40d18e8dbf99` (SQLSTATE `42501`: corrected defaults also restricted test-only pgTAP helpers),
and `8b5c10e5d8d9` (243 RLS assertions passed; index test incorrectly required all
composite FK columns in an index even where its leading device ID is already a unique PK).

The private-function failure was a genuine migration defect: per-schema default revokes
cannot remove PostgreSQL's global PUBLIC EXECUTE default. The fix revokes it for the
migration owner globally; future private functions must still explicitly revoke/review
privileges. Tests grant only assertion-helper access transactionally and roll it back.
The index assertion now checks an indexed leading FK equality column, without requiring
redundant copies of unique primary-key indexes. No RLS policy was weakened to pass a test.
[PostgreSQL default privileges](https://www.postgresql.org/docs/17/sql-alterdefaultprivileges.html).

### Other validation

`node --test tools/kr004/*.test.mjs`: **14 PASS**, 0 FAIL, 0 SKIP
(10 lifecycle tests plus four acceptance-counter regressions).
The fake-tool lifecycle tests prove local-target rejection, ownership checks, no credential
in arguments, cleanup after failure and primary-result preservation—not database isolation.

`node tools/validate.mjs` and `git diff --check`: **PASS**. The validator initially
counted only unchecked AC boxes and rejected an honestly partially completed issue.
Its minimum remains five; checked and unchecked numbered ACs now both count, with
regression tests for complete/partial/malformed/insufficient contracts. No security or
release-isolation check was removed. Required CI status is recorded in the issue/PR
publication update for the commit containing this evidence. No unchanged manual Android build was
repeated; the existing mandatory CI jobs are retained, with an additional real database job.

## Established scope versus remaining unknowns

**Established:** the 15-table local model builds; public read access is active-membership
scoped; all direct client writes deny; private tables/functions deny; actual non-owner,
non-BYPASSRLS anon/authenticated contexts pass two-household reads/writes/joins,
self-promotion, null-auth, missing-policy/grant and removed-member tests. Structural
constraints independently reject wrong-tenant/device/epoch/day/amount relationships.

**INFERRED:** this supplies the bounded local AC-1–4 evidence needed to review the next
KR-004 slice. It is not evidence for a functioning product or real-use approval.

**UNSPECIFIED / NOT RUN:** Auth HTTP/JWT verification, PostgREST, device gateway/HTTP
negative tests (AC-5), privileged atomic control/outbox and concurrent total-grant/version
checks (AC-6), complete before-client-exposure validation/deletion/retention (AC-7).
No product RPC exists yet; an absent control RPC and a rolled-back private test function
are explicitly tested, not presented as implemented gateway/control behavior.
PostgreSQL 17.6 here is the pinned Supabase test image, not a claim of latest upstream
patch level or approved production runtime. Hosting/region/deployment remain unselected.

AC-5–7, KR-005/006, production enforcement, private family use and distribution are
outside OD-42. KR-003 results and immutable artifacts are unchanged; visual work is paused.

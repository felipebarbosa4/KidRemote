# KR-008 AC-2 local APK update — 2026-09-13

- **Goal:** demonstrate actual versioned APK replacement preserves identity-bound accounting and validated transactional migration.
- **Context:** clean reporting baseline `609c0fd7fc1d114c7f36dba5451f4f0023714628`, Issue #8, draft PR #21, OD-46 app-update extension.
- **Constraints:** existing owned emulator only; no uninstall/clear in a prepare→update→verification window; no reducer/schema redesign, receipts/sync, enforcement, physical device or clock-policy change.
- **Done when:** actual versioned-install, restart, migration/crash/refusal evidence and exact APK identities are retained; affected checks/CI pass; Issue/PR reflect local versus physical/KR-009 boundaries.

## Method and source identities

Two same-signature debug app APKs are built from one recorded source commit, versionCode 1 and 2. They use the unchanged production Room schema 2 and reducer. They are laboratory builds, not the historical `609c0fd` binary or distributed releases. The CI bundle records source head, checkout merge head, package/version, APK SHA-256 and public signing-certificate digest; it contains no signing key or runtime data.

Scenario A prepares a real Room schema-2 ledger through the existing ChildAccounting/IdentityStore, then uses `adb install -r -t` for v1→v2. Scenario B separately prepares the same aggregate and disclosed legacy schema-1 SQL fixture already used by KR-008; real APK replacement precedes execution of the unchanged production `MIGRATION_1_2` under Room validation. Scenario B is not a claim that a historical schema-1 application was distributed.

The prepared aggregate has nonzero used time, policy version, absolute bonus, daily limit, manual lock, period key, cursor and HISTORY uncertainty. Private test oracles store digests/UID/PID/revision only, never raw identity/credential/policy contents. The encrypted identity digest must remain equal and decrypt successfully to the ledger epoch; UID, one ledger row and full payload digest must survive replacement before reconciliation. Controlled canonical fixtures then establish that covered intervals count zero and a new suffix counts once.

Migration observation wraps the exact production migration without changing its SQL or relaxing Room validation. A controlled kill occurs after its ALTER and before commit; old user_version, absence of the new column and identical payload demonstrate rollback. Retry and repeated opens distinguish two attempts (one interrupted) from exactly one committed migration. A missing migration path must throw while preserving schema/data. Actual APK downgrade is attempted without the downgrade override and must be rejected by Package Manager while v2/data remain installed.

Official contracts: [`adb install -r` retains data](https://developer.android.com/tools/adb); [Room migration validation and destructive-fallback implications](https://developer.android.com/training/data-storage/room/migrating-db-versions). Existing Room 2.8.5 remains selected; no dependency migration is introduced by current documentation examples.

## Initial checkpoint

**OBSERVED:** clean baseline, Issue #8 state and prior evidence inspected. Four focused Node/security tests, repository validation and whitespace checks pass. No accounting core, identity implementation, schema or migration SQL changed.

**UNSPECIFIED:** JVM/build, actual APK update, migration-crash and final CI results until executed. No AC closes from prepared tests alone.

## Scope boundary

Successful emulator results can demonstrate local AC-2 app-update persistence except KR-009 receipts. They cannot establish physical/OEM update/lifecycle support, physical p95, battery acceptance, real UsageStats accuracy or new clock recovery. Historical KR-007/KR-008 evidence remains unchanged.

## Preserved CI attempt 1

**OBSERVED:** [CI 34784704661](https://github.com/felipebarbosa4/KidRemote/actions/runs/34784704661), source `9a39de3`, failed in the new bundle verifier with `SIGNATURE_UNVERIFIED`. Main JVM/build/lint/APK audits, the v1 debug build and the other four jobs passed; no update bundle was published and no Android update was attempted. The old parser expected `Signer #1 certificate SHA-256 digest`. A local read-only verification using build-tools 37 on the preserved older lab APK emitted `V2 Signer: certificate SHA-256 digest` and exited 0. The verifier now accepts explicitly recognized formats while still rejecting missing/unknown/ambiguous certificate identities and requiring successful cryptographic verification.

**INFERRED:** the changed apksigner prefix explains the failed extraction; the failing CI did not retain its raw signer output. A regression uses the observed format and rejects two different digests. Independently, the post-update fixture was corrected before runtime to reconcile forward through its pending interval, then count a new suffix, instead of moving the controlled observation back to the old cursor. Production accounting/migration remain unchanged.

**OBSERVED metadata follow-up / partial CI:** while CI [34785098345](https://github.com/felipebarbosa4/KidRemote/actions/runs/34785098345) was starting for `c6fbc19`, read-only `aapt2` inspection of the preserved instrumentation APK showed empty versionCode/versionName (the separate test APK is unversioned). Its old regex would reject this. The follow-up records absent test version as null/empty while still requiring application v1/v2; a regression covers the exact observed shape. The in-progress CI was superseded by this correction before any runtime claim. Its final partial/cancellation state remains visible at the linked run; it is not counted as a successful validation.

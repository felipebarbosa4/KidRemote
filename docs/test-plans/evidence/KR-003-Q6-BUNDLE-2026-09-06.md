# KR-003 Q6 offline qualification handoff — 2026-09-06

Historical unexecuted handoff: OD-29 superseded Q6 with Q7 before physical execution. **Do not run this command.** The immutable bundle remains
unchanged as decision provenance; Q7 later became blocked on the unchanged Mi 8 before sample 1.

- **Goal:** Hand the owner one immutable command for the Mi 8's fresh offline 100-sample qualification.
- **Context:** Q5 physically passed its exact focused recovery route; Q6 retains the same APK bytes and adds only runner/protocol evidence controls.
- **Constraints:** Owner-operated Windows ADB; physical status Not run; no result pooling/resume, destructive action, production implementation or KR-004.
- **Done when:** Exact source/CI and every bundle byte are verified, then one owner execution produces a new preserved run directory.

Physical execution: **Not run**. This document records a candidate handoff, not an expiry pass, qualification result, broader support boundary,
production approval or Google Play acceptance.

## Immutable bundle

Source: `c9edbe5460fff8603a6ff4887114a572d297c89e`. Windows directory:
`C:\platform-tools\kr003-qualification-bundles\c9edbe5`.

| Artefact | Bytes | SHA-256 |
| --- | ---: | --- |
| `bundle.json` | 1,612 | `787181790fc8df8bb82ee5b218c38a51b3b57a9584ad2d534732b500d09a47d9` |
| `Start-KR003.ps1` | 51,758 | `9c736579a263d65878e4d4c3e0eb54aa9d26f9f932679f99899ac395d1d303cf` |
| `Clear-KR003-Lab.ps1` | 4,255 | `9b940d171145ccd76a4f1695310aacee3e1c7604b3040e8460d318d5a859a8b3` |
| `Qualification.psm1` | 23,617 | `3f3c15b1794dc06565b281238b0f5e0503a4249962989efc86abaab1441c511e` |
| `protocol.md` | 8,349 | `3f6bcadac2983889f684f7f5796d5641fd72bad2159e8b3f4e082390e0960f53` |
| `candidate.apk` | 2,675,201 | `5b27c891fe155ee4d26e4da68f8323f178f7116e73d8097ce07199e5800e318b` |
| `ordinary-fixture.apk` | 2,543,026 | `6653f10b527cc9a273a8c0ea045cd250f6978c1acfb8e92b00b701f6c14f84bb` |

All six nested payload hashes were independently recalculated and matched `bundle.json`. The runner, module and protocol are byte-identical to
the committed source; both APKs are byte-identical to the local verified build outputs. The APK hashes exactly match Q5 run
`run-20260906-171929-69a3abda`; packaging would have aborted on drift. Existing bundles/run evidence were not changed.

Bundle metadata: schema 1, protocol `KR003-Q6-MI8-OFFLINE-QUALIFICATION`, runner version 6, `diagnosticOnly=false`,
`requiresOffline=true`, `physicalExecution=NOT_RUN`. See the complete [Q6 contract](../KR-003-Q6-QUALIFICATION.md).

## Exact-source verification

[CI run 34062263290](https://github.com/felipebarbosa4/KidRemote/actions/runs/34062263290) passed all three jobs on exact source `c9edbe5`:

- 24/24 Android JVM tests, debug/release lint and assembly, and four merged-manifest/release-DEX audits;
- nine Node evidence/least-privilege tests and repository/link/whitespace validation;
- 72 PowerShell state/oracle/100-row assertions;
- 101 finalization/safety/bailout assertions on Linux PowerShell and again on native Windows PowerShell 5.1.

All PowerShell device calls in CI were stubs. Local packaging reran the Android build, repository validation and release audit. None of these
automated results is a physical trial.

## Exact owner command

With only the authorized Mi 8 connected, use one owner-operated Windows PowerShell terminal:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\c9edbe5\Start-KR003.ps1" -OfflineNetwork
```

The runner installs/verifies the exact APK pair, records original Wi-Fi/mobile-data flags, disables enabled radios and asks for physical offline
confirmation. On every normal stop it independently attempts sample-preserving lab CLEAR and radio restoration/readback. Keep the terminal open,
the device unlocked/interactive and the screen continuously visible.

Operator actions, in order:

1. Confirm offline state with **P** only after Wi-Fi/mobile data are visibly off and no other Internet route exists.
2. Observe the excluded calibration expiry for ten continuous seconds; press **P** only for persistent blocking with no flicker/disappearance/use.
3. Calibration safety: press Home physically and report **P** if blocked; open root Settings and report **P** if usable; attempt only Digital
   Wellbeing and report the expected **F** when restriction returns; use the overlay Settings button exactly once and report **P** only after root
   Settings remains usable for ten seconds; report **P** when automated ordinary re-entry is blocked; after CLEAR, tap the fixture test button once.
4. For each labelled sample 1–100, watch the entire post-expiry ten-second hold and enter one **P** only for persistent visible restriction.
5. Repeat step 3 for the final safety checkpoint after sample 100.

Use **F** for a genuine visible failure, **I** for a missed/invalid observation, or **Q** to stop. The only expected F is the explicitly labelled
Digital Wellbeing destination result inside each safety route; the runner understands that precondition. Any other F/I/Q, software invariant
failure, trace/health/configuration change, or inability to sustain Settings stops the qualification. Do not rerun a prompt or replace a row.

Estimated uninterrupted duration: **40–60 minutes**. Calibration and the two safety checkpoints add zero qualification samples. Attachment events
and internal latencies never generate P. Success requires exactly 100 new rows, zero physical/software failures, nearest-rank p95 at most 2,000 ms,
offline confirmation, both safety checkpoints, exact metrics, verified lab CLEAR/radio restoration and no finalization error.

## Emergency lab-only bailout

If the runner is forcibly interrupted or the restriction remains, use a second PowerShell terminal:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\platform-tools\kr003-qualification-bundles\c9edbe5\Clear-KR003-Lab.ps1"
```

This only snapshots/CLEARs the disposable lab timer and verifies unchanged latency samples. It does not clear data, uninstall, change permissions,
change radios or count as consumer recovery. A hard host/power kill cannot execute runner finalization; use `network-original.json` from the new
run directory to verify/restore radios manually if necessary.

## Interpretation

Exit zero means `PASSED_THIS_CONFIGURATION_ONLY` for this exact offline Mi 8/configuration and does not close KR-003. After execution, preserve the
new directory under `C:\platform-tools\kr003-qualification`; the agent will ingest it directly from `/mnt/c`. API 28/35/36, other OEMs,
lifecycle/tamper/safety rows, support-boundary approval and actual Play review remain outstanding.

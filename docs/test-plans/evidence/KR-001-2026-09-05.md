# KR-001 product-contract evidence — 2026-09-05

- **Goal:** Record objective completion evidence for the approved MVP state contract.
- **Context:** The owner directed the project to adopt the documented recommendations and proceed on 2026-09-05.
- **Constraints:** This is product/domain specification evidence, not Android, backend, Play or physical-device evidence.
- **Done when:** KR-001 acceptance criteria map to reviewed source and repeatable passing fixture checks.

## Acceptance evidence

| Criterion | Evidence |
| --- | --- |
| AC-1 | `unlock-clears-manual-only-at-zero` proves Unlock clears manual lock while zero remains TIME_EXPIRED. |
| AC-2 | `distinct-additions-and-transport-retry` produces 1,200 bonus seconds from two distinct grants plus one ignored retry. |
| AC-3 | Reset, limit reduction/increase, late grant, stale order and simultaneous lock/grant cases have numeric expected state. |
| AC-4 | OD-01–05/19 are approved with date and rationale in `docs/DECISIONS.md`; evidence-dependent boundaries remain explicit. |
| AC-5 | `docs/PRODUCT.md` explains counted time, per-device budget, household timezone and safe-surface dependency in parent language. |
| AC-6 | PRODUCT requirement traceability maps every MVP group and named non-feature set to test/issue IDs; PRIV-04 checks prohibited paths. |

Machine-readable cases: `docs/product-specs/STATE-MACHINE-CASES.json`.
Independent test-only evaluator: `tools/validate-state-fixtures.mjs`.

Cross-review against `packages/protocol/CONTRACT.md` and `docs/product-specs/BACKEND.md` found no state contradiction:
the fixtures model idempotent server acceptance/canonical grants, while child sync merges the resulting absolute dated bonus;
consumption remains local and is never reset by a server operation.

## Commands and observed results

```text
node tools/validate-state-fixtures.mjs
Approved state-machine fixtures passed.

node tools/validate.mjs
Planning validation passed, including approved state fixtures.

git diff --check
No output; exit 0.

In-memory failure injection changed the first expected remaining value to 999 seconds.
The validator rejected the inconsistent fixture and identified `unlock-clears-manual-only-at-zero`.
```

## Unrun evidence

No Android build, emulator, physical-device, backend, RLS, network, load or store-policy test was run for KR-001.
TIME/CMD cases requiring those levels remain assigned to their implementation issues.
